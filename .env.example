# Supabase (anon keys — safe for client)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# DeepSeek API (PRIVATE — do NOT commit)
# Get your key at https://platform.deepseek.com/
# This key is baked into client JS by Vite!
# In production, use a server-side proxy instead.
VITE_DEEPSEEK_KEY=
